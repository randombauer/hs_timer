def get_pixel_colour(i_x, i_y):
	import PIL.ImageGrab
	return PIL.ImageGrab.grab().load()[i_x, i_y]
 
def check_colour():
	colour = check_pixel_colour(x, y)
	if colour == yellow || colour == green
		return 1
	else:
		return 0

def count()
	while(get_pixel_colour(x_game, y_game)
		if (check_colour(x, y) == 1)
			my_counter += $OneSecond
		else:
			their_counter += 1
		overall_counter += 1

		
	
